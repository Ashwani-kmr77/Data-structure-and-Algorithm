#include<iostream>
#include<algorithm>
using namespace std;
int main(){
    int arr[8]={0,1,2,3,4,6,7,8};
    int n=8;
    int low=0;
    int ans;
    int high = n-1;
    while(low<=high){
        int mid = low+(high-low)/2;
        if(arr[mid]==mid) low= mid+1;
        else{
            ans=mid;
            high=mid-1;
        }
    }
    cout<<ans;

}
